function frameY() { let e = document.querySelector(".fullscreen-image div"), l = document.querySelector(".fullscreen-image div img").getBoundingClientRect(), r = document.querySelector(".gallery").getBoundingClientRect(); l.top + l.height >= r.bottom && (e.style.top = `${window.scrollY - l.height + r.bottom - 85}px`), window.scrollY < r.height - l.height && (e.style.top = `${window.scrollY}px`) } frameY(), window.addEventListener("scroll", frameY), window.addEventListener("resize", frameY); { let e = document.querySelectorAll(".gallery>.col"), l = 1; for (let r = 1; r < 20; r++) { let t = document.createElement("img"); t.src = `img/gallery/lq/${r}.jpg`, e[l - 1].appendChild(t), l % e.length ? l++ : l = 1 } } document.querySelectorAll(".gallery > .col").forEach((e, l) => { e.querySelectorAll("img").forEach((e, l) => { e.addEventListener("click", () => { document.querySelector(".fullscreen-image > div > img").src = `img/gallery/${e.src.split("/").pop()}`, document.querySelector(".fullscreen-image").style.display = "block" }) }) }), document.querySelector(".fullscreen-image>div>button").addEventListener("click", () => document.querySelector(".fullscreen-image").style.display = "none");