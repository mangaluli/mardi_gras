for (let i = 0; i < 8; i++)document.cookie[i]; function change_page(e) { document.querySelectorAll(".page").forEach((a, l) => e == l ? a.style.display = "flex" : a.style.display = "none"), document.querySelectorAll("nav>span").forEach((a, l) => e == l ? a.classList.add("active") : a.classList.remove("active")), document.querySelector("body").scrollIntoView(), document.cookie = `page=${e}` } document.querySelectorAll("nav>span").forEach((e, a) => e.addEventListener("click", () => change_page(a))), decodeURIComponent(document.cookie).split(";").forEach(e => { let a = e.split("="); "page" == a[0].trim() ? change_page(a[1]) : change_page(0) });